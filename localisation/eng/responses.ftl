remove-response = Removed *{ $role }* from your pronouns, { $name }.
add-response = Added *{ $role }* to your pronouns, { $name }.
prefix-response = Changed server prefix to `{ $prefix }`.
config-language-response = Changed server language to { $language-name } [{ $language-code }].
